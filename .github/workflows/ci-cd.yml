name: Money Transfer Pipeline - CI/CD with Shift-Left Testing

on:
  push:
    branches: [ main, develop, feature/** ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

env:
  JAVA_VERSION: '17'
  MAVEN_OPTS: '-Xmx3072m'

jobs:
  # Job 1: Static Code Analysis (Shift-Left)
  static-analysis:
    name: 🔍 Static Code Analysis
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./money-transfer    # ← ADD THIS!
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Full history for better analysis
    
    - name: ☕ Set up JDK
      uses: actions/setup-java@v4
      with:
        java-version: ${{ env.JAVA_VERSION }}
        distribution: 'temurin'
        cache: maven
    
    - name: 🔍 Run Checkstyle
      run: |
        echo "Running Checkstyle for code style violations..."
        mvn checkstyle:checkstyle || true
      continue-on-error: true
    
    - name: 🐛 Run SpotBugs
      run: |
        echo "Running SpotBugs for potential bugs..."
        mvn com.github.spotbugs:spotbugs-maven-plugin:4.8.3.0:check || true
      continue-on-error: true
    
    - name: 📊 Run PMD
      run: |
        echo "Running PMD for code quality issues..."
        mvn pmd:pmd pmd:cpd || true
      continue-on-error: true
    
    - name: 📤 Upload Analysis Reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: static-analysis-reports
        path: |
          money-transfer/target/checkstyle-result.xml
          money-transfer/target/spotbugsXml.xml
          money-transfer/target/pmd.xml
        retention-days: 30